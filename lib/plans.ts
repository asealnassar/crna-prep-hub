/**
 * Plan limits.
 *
 * Single source of truth: the interview page enforces this number and the
 * pricing page advertises it, so the two can never drift apart. Raising it
 * here changes both.
 */
export const FREE_INTERVIEW_ALLOWANCE = 3
